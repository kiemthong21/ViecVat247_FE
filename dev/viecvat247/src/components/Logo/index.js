import React from "react";
import images from "~/assets/images";

const Logo = () => {
    return <img src={images.logo.default} alt="" style={{ width: "50px", height: "50px" }} />;
};

export default Logo;
